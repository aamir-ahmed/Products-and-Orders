name: Update Postman SpecHub Spec

on:
  workflow_dispatch:
  push:
    paths:
      - '**/*.yaml'
      - '**/*.json'

jobs:
  update-spec:
    runs-on: ubuntu-latest
    steps:
      - name: "🔍 Checkout main repo"
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: "Install dependencies"
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: "🔍 Find and Load Swagger File"
        id: find_swagger
        run: |
          SWAGGER_FILE=$(find . -maxdepth 2 -type f \( -iname "*.yaml" -o -iname "*.yml" \) | head -n 1)
          if [[ -z "$SWAGGER_FILE" ]]; then
            echo "❌ No Swagger file found in the repository."
            exit 1
          else
            echo "✅ Found Swagger File: $SWAGGER_FILE"
            echo "SWAGGER_PATH=$SWAGGER_FILE" >> $GITHUB_ENV
            SWAGGER_CONTENT=$(jq -Rs . < "$SWAGGER_FILE")
            echo "SWAGGER_CONTENT=$SWAGGER_CONTENT" >> $GITHUB_ENV
          fi

      - name: 📦 Clone config branch
        run: |
          git clone --single-branch --branch config https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }} config-branch

      - name: 📤 Read existing POSTMAN IDs
        id: read_meta
        run: |
          cd config-branch
          if [ -f config/spec-meta.json ]; then
            POSTMAN_SPEC_ID=$(jq -r '.POSTMAN_SPEC_ID' config/spec-meta.json)
            POSTMAN_COLLECTION_ID=$(jq -r '.POSTMAN_COLLECTION_ID' config/spec-meta.json)
            echo "POSTMAN_SPEC_ID=$POSTMAN_SPEC_ID" >> $GITHUB_ENV
            echo "POSTMAN_COLLECTION_ID=$POSTMAN_COLLECTION_ID" >> $GITHUB_ENV
            echo "✅ Loaded existing IDs"
          else
            echo "⚠️ No spec-meta.json, initializing"
            mkdir -p config
            echo '{"POSTMAN_SPEC_ID":"","POSTMAN_COLLECTION_ID":"","LAST_UPDATED":""}' > config/spec-meta.json
          fi

      - name: 🧪 Upload Spec and Generate Collection
        if: env.POSTMAN_SPEC_ID == ''
        id: upload_and_generate
        env:
          POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
          POSTMAN_WORKSPACE_ID: ${{ secrets.POSTMAN_WORKSPACE_ID }}
          SWAGGER_PATH: ${{ env.SWAGGER_PATH }}
        run: |
          RAW_YAML=$(cat "$SWAGGER_PATH")
          SPEC_CONTENT=$(echo "$RAW_YAML" | jq -Rs .)

          CREATE_SPEC_PAYLOAD=$(jq -n \
            --arg name "Products & Orders" \
            --arg type "OPENAPI:3.0" \
            --arg path "index.yaml" \
            --argjson content "$SPEC_CONTENT" \
            '{ name: $name, type: $type, files: [{ path: $path, content: $content }] }')

          response=$(curl -s -w "\n%{http_code}" --location --request POST \
            "https://api.getpostman.com/specs?workspaceId=${POSTMAN_WORKSPACE_ID}" \
            --header "Content-Type: application/json" \
            --header "X-API-Key: ${POSTMAN_API_KEY}" \
            --data "$CREATE_SPEC_PAYLOAD")

          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)

          if [[ "$status" == "200" || "$status" == "201" ]]; then
            echo "✅ Spec created successfully!"
            spec_id=$(echo "$body" | jq -r '.id')
            echo "POSTMAN_SPEC_ID=$spec_id" >> $GITHUB_ENV
            echo "$spec_id" > config-branch/config/.spec_id
          else
            echo "❌ Failed to create spec (HTTP $status):"
            echo "$body"
            exit 1
          fi

          sleep 5

          GENERATE_COLLECTION_PAYLOAD=$(jq -n \
            --arg name "Products & Orders" \
            --arg parametersResolution "Example" \
            '{ name: $name, options: { parametersResolution: $parametersResolution } }')

          response=$(curl -s -w "\n%{http_code}" --location --request POST \
            "https://api.getpostman.com/specs/${spec_id}/generations/collection" \
            --header "Content-Type: application/json" \
            --header "X-API-Key: ${POSTMAN_API_KEY}" \
            --data "$GENERATE_COLLECTION_PAYLOAD")

          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)

          if [[ "$status" == "200" || "$status" == "201" || "$status" == "202" ]]; then
            echo "✅ Collection generated successfully!"
            collection_id=$(echo "$body" | jq -r '.taskId')
          else
            echo "❌ Failed to generate collection (HTTP $status):"
            echo "$body"
            exit 1
          fi

          sleep 5

          collection_response=$(curl -s -w "\n%{http_code}" --location --request GET \
            "https://api.getpostman.com/specs/${spec_id}/generations/collection" \
            --header "Content-Type: application/json" \
            --header "X-API-Key: ${POSTMAN_API_KEY}")

          collection_body=$(echo "$collection_response" | head -n -1)
          collection_status=$(echo "$collection_response" | tail -n1)

          if [[ "$collection_status" == "200" || "$collection_status" == "201" || "$collection_status" == "202" ]]; then
            echo "✅ Collection information captured!"
            collection_id=$(echo "$collection_body" | jq -r '.collections[0].id')
            echo "POSTMAN_COLLECTION_ID=$collection_id" >> $GITHUB_ENV
            echo "$collection_id" > config-branch/config/.collection_id
          else
            echo "❌ Failed to get collection (HTTP $collection_status):"
            echo "$collection_body"
            exit 1
          fi

      - name: 📤 Upload Swagger Spec to Postman
        if: env.POSTMAN_SPEC_ID != ''
        id: upload_spec
        env:
          POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
          SWAGGER_PATH: ${{ env.SWAGGER_PATH }}
          POSTMAN_SPEC_ID: ${{ env.POSTMAN_SPEC_ID }}
        run: |
          RAW_YAML=$(cat "$SWAGGER_PATH")
          SPEC_CONTENT=$(echo "$RAW_YAML" | jq -Rs .)
          echo "{\"content\": ${SPEC_CONTENT}"} > payload.json

          response=$(curl -s -w "\n%{http_code}" --location --request PATCH "https://api.getpostman.com/specs/${POSTMAN_SPEC_ID}/files/index.yaml" \
            --header "Content-Type: application/json" \
            --header "X-API-Key: ${POSTMAN_API_KEY}" \
            --data @payload.json)

          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)

          if [[ "$status" != "200" ]]; then
            echo "❌ Failed to update spec: $status"
            echo "$body"
            exit 1
          fi
          echo "✅ Spec updated"

      - name: 🔄 Sync Postman Collection
        if: env.POSTMAN_SPEC_ID != '' && env.POSTMAN_COLLECTION_ID != ''
        id: sync_collection
        env:
          POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
          POSTMAN_COLLECTION_ID: ${{ env.POSTMAN_COLLECTION_ID }}
          POSTMAN_SPEC_ID: ${{ env.POSTMAN_SPEC_ID }}
        run: |
          response=$(curl -s -w "\n%{http_code}" --location --request PUT \
            "https://api.getpostman.com/collections/${POSTMAN_COLLECTION_ID}/synchronizations?specId=${POSTMAN_SPEC_ID}" \
            --header "X-API-Key: ${POSTMAN_API_KEY}" \
            --header "Content-Type: application/json")

          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)

          if [[ "$status" == "200" || "$status" == "201" || "$status" == "202" ]]; then
            echo "✅ Collection synced"
          elif [[ "$status" == "400" && "$body" == *"already in sync"* ]]; then
            echo "✅ Already in sync"
          else
            echo "❌ Sync failed: $status"
            echo "$body"
            exit 1
          fi

      - name: 💾 Update & Commit spec-meta.json
        run: |
          cd config-branch
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          jq -n \
            --arg specId "${{ env.POSTMAN_SPEC_ID }}" \
            --arg collectionId "${{ env.POSTMAN_COLLECTION_ID }}" \
            --arg updated "$TIMESTAMP" \
            '{ POSTMAN_SPEC_ID: $specId, POSTMAN_COLLECTION_ID: $collectionId, LAST_UPDATED: $updated }' > config/spec-meta.json

          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add config/spec-meta.json
          git commit -m "🔄 Update Postman IDs at $TIMESTAMP"
          git push origin config
